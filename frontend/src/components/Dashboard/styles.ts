import styled from "styled-components";
import * as BS from "react-bootstrap";

export const Card = styled(BS.Card)`
  text-align: center;
  .card-title {
    font-size: 3rem;
    margin-top: 1rem;
  }
  margin-top: 1rem;
`;
export const IconDash = styled.div`
  text-align: left;
  font-size: 7rem;
  margin-top: -2rem;
  color: #8704cb30;
`;
